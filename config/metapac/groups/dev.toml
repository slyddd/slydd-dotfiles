arch = { packages = [
  # Runtime
  "bun",

  # Development tools
  "neovim",
  "zed",
  "cursor-bin",

  # CLI
  "git",
  "github-cli",
] }

# Bun packages (development tools)
bun = { packages = ["eslint", "prettier"] }
